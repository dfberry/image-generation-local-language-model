# SDXL Hugging Face model marker

This lightweight Azure ML model asset is a marker for the Hugging Face SDXL
model that `score.py` loads at runtime:

- model: `stabilityai/stable-diffusion-xl-base-1.0`
- revision: `main`
- precision: `fp16`

The model weights are not stored in this repository.
